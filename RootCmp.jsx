const { useState } = React

export function RootCmp() {

    const [page, setPage] = useState(null)

    return (
        <section className="app">

            <main>

            </main>
        </section>
    )
}